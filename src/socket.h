/** @brief Renvoie l'information brut recupéré par connection socket sur le server placé sur le robot d'ip passé en parametre
* @param ip Ip du robot ou se trouve le serveur
* @return L'information brut recupérér du serveur
*/
char * socketRecupRobot(char * ip);