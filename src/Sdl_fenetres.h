#include <SDL2/SDL.h>
#include <stdio.h>
#include <time.h>
#include "Sdl_init.h"
#include "partie.h"



/**
	@brief Affiche que la partie est en attengte de joueur pour démarrer
	@return retourne 0 si on doit quitter, ou 2 si on passe au menu suivant
*/
int loading();

/**
	@brief Affiche que la partie est en attengte de joueur pour démarrer
	@return retourne 0 si on doit quitter, ou 2 si on passe au menu suivant
*/
int partie();
